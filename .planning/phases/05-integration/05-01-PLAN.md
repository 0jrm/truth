---
phase: 05-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - truth/store/paths.py
  - truth/index/db.py
  - truth/index/indexer.py
  - truth/serve.py
  - truth/export.py
  - truth/cli.py
  - truth/static/inspector.html
  - truth/static/vendor/sql-wasm.js
  - truth/static/vendor/sql-wasm.wasm
autonomous: true
requirements:
  - CLI-01
  - CLI-02
user_setup: []
must_haves:
  truths:
    - "D-01: truth serve runs index_all on startup then daemon watcher until Ctrl+C"
    - "D-02: Ctrl+C stops observer and joins threads cleanly"
    - "D-03: truth serve does not start HTTP server (ThreadingHTTPServer removed)"
    - "D-04/D-13: truth index runs one-shot index_all; serve also indexes on startup"
    - "D-05: sql.js vendored offline in truth/static/vendor (no CDN)"
    - "D-06: truth export copies inspector.html + wasm assets to notes root"
    - "D-07: browser refresh re-fetches memory.db (no inspector.json pipeline)"
    - "D-08/D-12: default TRUTH_DB_PATH is notes/memory.db; only two env vars"
    - "D-09/D-10: notes and edges tables populated at index; deleted on file delete"
    - "D-11: env-only config — no truth.toml in v1"
    - "D-14: no truth write CLI subcommand"
    - "D-19: HTTP serve from Phase 4 superseded by headless serve + sql.js"
  artifacts:
    - path: truth/serve.py
      provides: "Headless serve loop (index + watcher)"
      contains: "def run_serve"
    - path: truth/cli.py
      provides: "serve, index, export subcommands"
      contains: '"index"'
    - path: truth/index/db.py
      provides: "notes + edges schema"
      contains: "CREATE TABLE IF NOT EXISTS notes"
    - path: truth/static/inspector.html
      provides: "sql.js browser inspector"
      contains: "initSqlJs"
  key_links:
    - from: truth/index/indexer.py
      to: truth/store/links.py
      via: "extract_links for edges table"
      pattern: "extract_links"
    - from: truth/export.py
      to: truth/static/
      via: "copy HTML + vendor wasm to notes root"
      pattern: "shutil.copy"
    - from: truth/serve.py
      to: truth/index/watcher.py
      via: "start_watcher(block=False)"
      pattern: "start_watcher"
---

<objective>
Unified CLI: env config with DB colocated in notes root, indexer `notes`/`edges` tables, headless `truth serve`, `truth index`, `truth export`, and sql.js static inspector.

Purpose: Single command runs the full index loop; browser reads `memory.db` offline without HTTP.
Output: Runnable `truth serve` / `truth index` / `truth export` with refactored static inspector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration/05-CONTEXT.md
@.planning/phases/05-integration/05-RESEARCH.md
@truth/index/indexer.py
@truth/index/watcher.py
@truth/serve.py
@truth/cli.py
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path traversal via env paths | Low | Resolve paths; document trusted local use |
| sql.js loading untrusted DB | Low | Local-only; user opens own notes root |
| Orphan watcher threads on exit | Low | observer.stop() + join on SIGINT |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Config + schema (notes/edges tables, DB default in notes root)</name>
  <files>truth/store/paths.py, truth/index/db.py</files>
  <read_first>
    - truth/store/paths.py
    - truth/index/db.py
    - .planning/phases/05-integration/05-CONTEXT.md
  </read_first>
  <action>
- Add `def db_path() -> Path` to `truth/store/paths.py` (or re-export from db): when `TRUTH_DB_PATH` unset, return `notes_root() / "memory.db"`; when set, use env value resolved
- Update `truth/index/db.py` to import `db_path` from `truth/store/paths.py` (remove duplicate env read)
- Extend `_SCHEMA` in db.py:
  - `notes(path TEXT PRIMARY KEY, type TEXT, title TEXT, mtime REAL NOT NULL)`
  - `edges(source TEXT, target TEXT, label TEXT NOT NULL DEFAULT '', PRIMARY KEY (source, target, label))`
  - `CREATE INDEX IF NOT EXISTS edges_target ON edges(target)`
- `init_schema` unchanged pattern (executescript + commit)
- ponytail: document breaking default move in code comment
  </action>
  <acceptance_criteria>
    - truth/index/db.py contains `CREATE TABLE IF NOT EXISTS notes`
    - truth/store/paths.py contains `TRUTH_DB_PATH` and default under notes root
    - python -c "from truth.index.db import init_schema, open_db; init_schema(open_db())" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Indexer writes notes + edges; delete cleans graph rows</name>
  <files>truth/index/indexer.py</files>
  <read_first>
    - truth/index/indexer.py
    - truth/store/links.py
    - truth/index/db.py
  </read_first>
  <action>
In `index_file` after successful parse (inside transaction):
- Upsert `notes` row: path=rel, type, title, mtime=path.stat().st_mtime
- `DELETE FROM edges WHERE source = ?` then insert edges from `extract_links(resolved, root)` with source/target as relative paths under notes root (use existing `_relative_path` for targets)
In `delete_file_from_index`:
- `DELETE FROM notes WHERE path = ?`
- `DELETE FROM edges WHERE source = ? OR target = ?`
- Existing chunk/files cleanup unchanged
Add `if __name__` assertion: after index_all on repo notes, `SELECT COUNT(*) FROM notes` >= 3
  </action>
  <acceptance_criteria>
    - truth/index/indexer.py contains `INSERT INTO notes`
    - truth/index/indexer.py contains `DELETE FROM edges WHERE source`
    - python -m truth.index.indexer exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Headless serve + CLI index/export</name>
  <files>truth/serve.py, truth/export.py, truth/cli.py</files>
  <read_first>
    - truth/serve.py
    - truth/index/indexer.py
    - truth/index/watcher.py
    - truth/cli.py
    - .planning/phases/05-integration/05-CONTEXT.md
  </read_first>
  <action>
- Replace `truth/serve.py` HTTP server with `run_serve()`:
  - Print startup line with notes root + db path
  - `index_all()` on startup
  - `observer = start_watcher(block=False)`
  - Block main thread until KeyboardInterrupt; on exit: `observer.stop(); observer.join()`; print "Stopped."
  - Remove ThreadingHTTPServer, InspectorHandler, run_server (HTTP path deleted per D-03)
- New `truth/export.py`: `export_inspector(dest: Path | None = None) -> Path` copies `inspector.html`, `static/vendor/sql-wasm.js`, `static/vendor/sql-wasm.wasm` into notes root (default `notes_root()`); return dest dir
- `truth/cli.py`:
  - `serve` subcommand: no --host/--port; calls `run_serve()`
  - `index` subcommand: runs `index_all()`, prints `indexed_files=N`
  - `export` subcommand: runs export_inspector, prints destination path
  </action>
  <acceptance_criteria>
    - truth/serve.py contains `def run_serve`
    - truth/serve.py does not contain `ThreadingHTTPServer`
    - truth/cli.py contains subparser `"index"`
    - truth/export.py contains `def export_inspector`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Vendor sql.js + refactor inspector.html</name>
  <files>truth/static/vendor/sql-wasm.js, truth/static/vendor/sql-wasm.wasm, truth/static/inspector.html</files>
  <read_first>
    - truth/static/inspector.html
    - truth/inspect/tree.py
    - truth/inspect/links.py
    - truth/inspect/changes.py
    - .planning/phases/05-integration/05-RESEARCH.md
  </read_first>
  <action>
- Download sql.js wasm build (pin version in comment) into `truth/static/vendor/sql-wasm.js` and `sql-wasm.wasm`
- Refactor `inspector.html`:
  - Load sql.js from `vendor/sql-wasm.js` (relative path for export colocation)
  - On load: `fetch('memory.db')`, open with initSqlJs
  - Tree panel: `SELECT path, type, title FROM notes ORDER BY path` (compute depth from path parts in JS)
  - Links panel: query `edges` for outgoing/incoming by selected path
  - Changes panel: `SELECT ts, op, path FROM events ORDER BY id DESC LIMIT 20`
  - Keep minimal dark UI from Phase 4; add Refresh button that re-fetches memory.db
  - No `/api/*` fetch calls remain
  </action>
  <acceptance_criteria>
    - truth/static/inspector.html contains `initSqlJs`
    - truth/static/inspector.html contains `memory.db`
    - truth/static/inspector.html does not contain `/api/tree`
    - truth/static/vendor/sql-wasm.wasm exists (non-empty file)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `TRUTH_NOTES_ROOT=notes truth index` prints indexed count and creates notes/memory.db (or configured path)
2. `truth export` copies inspector + vendor files into notes root
3. `timeout 2 truth serve` or manual Ctrl+C test: starts without import error, indexes, stops cleanly
4. After index: `sqlite3 notes/memory.db "SELECT COUNT(*) FROM notes"` >= 3
</verification>

<success_criteria>
- CLI-01: `truth serve` starts indexer + watcher together
- CLI-02: env vars TRUTH_NOTES_ROOT and TRUTH_DB_PATH work; no truth.toml required
- Phase 4 CLI commands still work unchanged
</success_criteria>
