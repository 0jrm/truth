---
phase: 05-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
  - notes/agent-workflow.md
  - notes/log-convention.md
  - notes/okf-format.md
  - prompts/system.md
  - truth/smoke.py
  - tests/test_integration.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
user_setup: []
must_haves:
  truths:
    - "D-15: README updated in place (not rewritten); truth serve/index quickstart"
    - "D-16: Ollama tool_schemas section preserved in README"
    - "D-17: notes/ expanded with agent-workflow and log-convention linked concepts"
    - "D-18: README + smoke script + pytest integration test cover e2e flow"
    - "D-20: truth index deferral resolved; truth write CLI still declined"
    - "End-to-end flow verified: write note → changes → searchable → tree/links"
  artifacts:
    - path: README.md
      provides: "Install, quickstart, config, Ollama hookup"
      contains: "truth serve"
    - path: truth/smoke.py
      provides: "Standalone e2e smoke script"
      contains: "memory_write"
    - path: tests/test_integration.py
      provides: "pytest integration guard"
      contains: "def test_"
  key_links:
    - from: truth/smoke.py
      to: truth/tools/
      via: "memory_write + memory_search + CLI subprocess"
      pattern: "memory_write"
    - from: README.md
      to: prompts/system.md
      via: "agent wiring section"
      pattern: "prompts/system.md"
---

<objective>
Documentation, expanded sample brain, and end-to-end verification for Phase 5 integration.

Purpose: New users can install, run `truth serve`, inspect via CLI and browser, and wire agents per README.
Output: Updated README, 2–3 new linked notes, smoke script, pytest integration test, ROADMAP alignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration/05-CONTEXT.md
@README.md
@prompts/system.md
@notes/okf-format.md
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Smoke test writes outside temp dir | Low | Use TemporaryDirectory + env vars |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Expand sample notes with cross-links</name>
  <files>notes/agent-workflow.md, notes/log-convention.md, notes/okf-format.md, notes/hybrid-search.md</files>
  <read_first>
    - notes/okf-format.md
    - notes/hybrid-search.md
    - notes/sqlite-vectors.md
    - truth/store/links.py
  </read_first>
  <action>
Create 2 new OKF notes with valid frontmatter (type, title, timestamp):
- `notes/agent-workflow.md` — search-before-answer / write-after-learn loop; link to [log convention](log-convention.md) and [hybrid search](hybrid-search.md)
- `notes/log-convention.md` — OKF log.md append behavior; link back to [agent workflow](agent-workflow.md) and [OKF format](okf-format.md)
Add at least one incoming link from existing `notes/okf-format.md` to one new note
Ensure all notes remain valid for indexer (type in frontmatter)
  </action>
  <acceptance_criteria>
    - notes/agent-workflow.md exists with `type:` in frontmatter
    - notes/log-convention.md exists with markdown link to agent-workflow.md
    - python -m truth.store.links exits 0 (seed edges >= 2 still passes with expanded graph)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update README and system prompt</name>
  <files>README.md, prompts/system.md, .planning/ROADMAP.md</files>
  <read_first>
    - README.md
    - prompts/system.md
    - .planning/ROADMAP.md
    - .planning/phases/05-integration/05-CONTEXT.md
  </read_first>
  <action>
Update README (preserve tone/structure — edit facts, do not rewrite from scratch):
- Quick start: replace `python -m truth.index.indexer` with `truth index`; replace watcher one-liner with `truth serve`
- Config table: default `TRUTH_DB_PATH` is `{notes_root}/memory.db`; note migration from project-root memory.db
- Add "Browser inspector" section: `truth export` then open `file://.../inspector.html`; refresh to see updates
- Mark Phase 4 complete in roadmap checklist; Phase 5 items checked where done
- Remove `truth.toml` from Phase 5 checkbox; note env-only v1
- Project structure: memory.db under notes/
Update `prompts/system.md`: reference `truth serve` for live index (not python -m watcher)
Update `.planning/ROADMAP.md` Phase 5 success criteria to match CONTEXT (headless serve, sql.js inspector, env config — not HTTP+inspector together)
  </action>
  <acceptance_criteria>
    - README.md contains `truth serve`
    - README.md contains `truth export`
    - README.md does not contain `python -m truth.index.indexer` in quick start
    - prompts/system.md contains `truth serve`
    - .planning/ROADMAP.md Phase 5 success criteria mention env vars (not truth.toml as required)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Smoke script + pytest integration test</name>
  <files>truth/smoke.py, tests/test_integration.py, pyproject.toml</files>
  <read_first>
    - truth/tools/__init__.py
    - truth/cli.py
    - truth/index/indexer.py
    - .planning/phases/05-integration/05-CONTEXT.md
  </read_first>
  <action>
- `truth/smoke.py`: runnable script using TemporaryDirectory:
  - Set TRUTH_NOTES_ROOT + TRUTH_DB_PATH to temp dir
  - Copy or create minimal seed note
  - Run index_all(); memory_write a new note; index_all() again
  - Assert memory_search finds written content
  - subprocess: `truth tree`, `truth changes -n 5` exit 0
  - Print "smoke ok" on success; exit 1 on failure
- `tests/test_integration.py`: pytest test wrapping same flow (import smoke helpers or duplicate minimal assertions)
- Add optional dev dependency `[project.optional-dependencies] test = ["pytest>=7"]` in pyproject.toml if not present
- `if __name__` in smoke.py for direct run
  </action>
  <acceptance_criteria>
    - python truth/smoke.py exits 0
    - pytest tests/test_integration.py exits 0 (after pip install -e ".[test]" or pytest available)
    - truth/smoke.py contains `memory_write`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. README quickstart commands match implemented CLI
2. `python truth/smoke.py` passes from repo root
3. `pytest tests/test_integration.py -q` passes
4. Manual: `truth index && truth tree && truth export` — 5+ notes visible
</verification>

<success_criteria>
- End-to-end demo path documented and automated
- CLI-01 and CLI-02 validated via smoke + README
- Phase 5 ROADMAP success criteria aligned with shipped behavior
</success_criteria>
