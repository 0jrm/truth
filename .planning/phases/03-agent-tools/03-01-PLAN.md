---
phase: 03-agent-tools
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - truth/tools/__init__.py
  - truth/tools/write.py
autonomous: true
requirements:
  - TOOL-01
user_setup: []
must_haves:
  truths:
    - "from truth.tools import memory_search, memory_write works in REPL"
    - "memory_write creates .md under notes/ with valid frontmatter"
    - "Path traversal (..) and non-.md paths are rejected"
    - "Written file is searchable after watcher cycle (or index_file in self-check)"
  artifacts:
    - path: truth/tools/__init__.py
      provides: "Public agent API exports"
      contains: "memory_search"
    - path: truth/tools/write.py
      provides: "memory_write implementation"
      contains: "def memory_write"
  key_links:
    - from: truth/tools/write.py
      to: truth/store/frontmatter.py
      via: "format_note + validate_frontmatter"
      pattern: "format_note"
    - from: truth/tools/__init__.py
      to: truth/index/search.py
      via: "re-export memory_search"
      pattern: "memory_search"
---

<objective>
Agent write path: memory_write writes OKF markdown to notes/ with frontmatter enforcement and path safety. Re-export memory_search from index layer.

Purpose: First agent-facing write API — markdown truth, watcher handles index.
Output: `truth.tools` importable with memory_search and memory_write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-tools/03-CONTEXT.md
@.planning/phases/03-agent-tools/03-RESEARCH.md
@truth/store/frontmatter.py
@truth/store/paths.py
@truth/index/search.py
@VISION.MD
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path traversal via memory_write | Medium | Resolve path, verify under notes_root |
| Writing outside .md | Low | Reject non-.md suffix |
| Invalid frontmatter persisted | Low | validate_frontmatter before write |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold and search re-export</name>
  <files>truth/tools/__init__.py</files>
  <action>
Create `truth/tools/__init__.py`:
- Re-export `memory_search` from `truth.index.search`
- Re-export `memory_write` from `truth.tools.write` (stub until Task 2)
- `__all__ = ["memory_search", "memory_write"]`
  </action>
  <verify>
    <automated>python -c "from truth.tools import memory_search; print(memory_search('test', k=1))"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: memory_write implementation</name>
  <files>truth/tools/write.py</files>
  <action>
Implement:
- `_safe_note_path(rel: str, root: Path) -> Path` — resolve, verify under root, require .md
- `memory_write(path, content, *, type="Note", title=None, summary=None) -> Path`
  - Parse content: if starts with `---\n`, use split_frontmatter + validate_frontmatter; else treat as body
  - Build final text via format_note
  - mkdir parents, write_text UTF-8
  - Return resolved path
- Do NOT call index directly (watcher handles it)

Add `if __name__ == '__main__'` self-check in temp dir: write note, index_file manually, memory_search finds it.
  </action>
  <verify>
    <automated>python -m truth.tools.write 2>&1 | grep -q ok</automated>
  </verify>
</task>

</tasks>

<verification>
1. `from truth.tools import memory_search, memory_write` succeeds
2. `memory_write("test/uat.md", "Hello world")` creates file with type: Note frontmatter
3. `memory_write("../etc/passwd", "x")` raises ValueError
4. After index_file or watcher, memory_search finds written content
</verification>

<success_criteria>
- TOOL-01 satisfied
- ROADMAP criteria 1 and 2 (import + searchable write) met
- No SQLite writes from tools layer
</success_criteria>
