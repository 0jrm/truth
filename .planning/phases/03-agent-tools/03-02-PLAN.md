---
phase: 03-agent-tools
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - truth/tools/write.py
  - truth/tools/schemas.py
  - truth/tools/__init__.py
  - prompts/system.md
autonomous: true
requirements:
  - TOOL-02
  - TOOL-03
  - TOOL-04
user_setup: []
must_haves:
  truths:
    - "memory_write appends timestamped entry to notes/log.md"
    - "tool_schemas() returns memory_search and memory_write JSON schemas"
    - "prompts/system.md documents search-before-answer / write-after-learn"
  artifacts:
    - path: truth/tools/schemas.py
      provides: "Function-calling schema export"
      contains: "def tool_schemas"
    - path: prompts/system.md
      provides: "Agent system prompt template"
      contains: "memory_search"
  key_links:
    - from: truth/tools/write.py
      to: notes/log.md
      via: "append on write"
      pattern: "log.md"
---

<objective>
OKF log.md append on write, JSON tool schemas for function calling, and system prompt template.

Purpose: Complete agent integration contract — human changelog + machine tool defs + behavioral prompt.
Output: log append wired into memory_write; tool_schemas(); prompts/system.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-tools/03-CONTEXT.md
@.planning/phases/03-agent-tools/03-RESEARCH.md
@truth/tools/write.py
@VISION.MD
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| log.md path injection | Low | Append only validated relative path from memory_write |
| Unbounded log growth | Low | ponytail: no rotation in v1; document manual trim |

Block on: high — none.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: log.md append helper</name>
  <files>truth/tools/write.py</files>
  <action>
Add `_append_log(root: Path, rel_path: str, summary: str) -> None`:
- Target `{root}/log.md`
- If missing, create with frontmatter `type: Log` and title
- Append line: `- {ISO8601 UTC} **{rel_path}** — {summary}\n`
- Call from memory_write after successful write; summary defaults to first body line (max 120 chars)

Ensure log.md itself is a normal note (can be indexed).
  </action>
  <verify>
    <automated>python -c "from truth.tools.write import _append_log; from pathlib import Path; import tempfile; d=Path(tempfile.mkdtemp()); _append_log(d,'a.md','test'); assert (d/'log.md').exists()"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Tool schemas</name>
  <files>truth/tools/schemas.py, truth/tools/__init__.py</files>
  <action>
Implement `tool_schemas() -> list[dict]` with OpenAI-compatible function definitions for:
- memory_search(query: str, k: int = 5)
- memory_write(path: str, content: str, type: str = "Note", title: str | None, summary: str | None)

Include clear descriptions for agent use. Export from truth.tools.
  </action>
  <verify>
    <automated>python -c "from truth.tools import tool_schemas; assert len(tool_schemas())==2"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: System prompt template</name>
  <files>prompts/system.md</files>
  <action>
Create `prompts/system.md` per VISION.MD and ROADMAP:
- Persistent markdown memory
- BEFORE answering: memory_search
- AFTER learning: memory_write
- Frontmatter type requirement
- Note watcher/indexer must be running for live search
- Brief example tool call flow

Keep under ~40 lines, copy-paste ready for Ollama system prompt.
  </action>
  <verify>
    <automated>test -f prompts/system.md && grep -q memory_search prompts/system.md</automated>
  </verify>
</task>

</tasks>

<verification>
1. memory_write creates/updates log.md with new list entry
2. tool_schemas() returns valid JSON-schema structures
3. prompts/system.md exists and mentions both tools
</verification>

<success_criteria>
- TOOL-02, TOOL-03, TOOL-04 satisfied
- ROADMAP criteria 3, 4, 5 met
</success_criteria>
