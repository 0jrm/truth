---
phase: 04-memory-inspector
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - truth/serve.py
  - truth/static/inspector.html
  - truth/cli.py
autonomous: true
requirements:
  - INSPECT-05
user_setup: []
must_haves:
  truths:
    - "truth serve exposes JSON API for tree, links, changes, graph"
    - "truth serve serves static inspector.html with three panels"
  artifacts:
    - path: truth/serve.py
      provides: "stdlib HTTP server + JSON API"
      contains: "def run_server"
    - path: truth/static/inspector.html
      provides: "Browser inspector UI"
      contains: "fetch('/api/tree')"
  key_links:
    - from: truth/serve.py
      to: truth/inspect/tree.py
      via: "reuse build_tree, note_links, etc."
      pattern: "build_tree"
---

<objective>
Optional browser inspector: stdlib HTTP server with JSON API and single static HTML page (tree, links, changes panels).

Purpose: Same data as CLI, viewable in browser without build step.
Output: `truth serve` on default port 8765.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@truth/inspect/
@.planning/PROJECT.md
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Unauthenticated local server exposure | Low | Bind 127.0.0.1 only; document local-only |
| Path traversal via API | Medium | Reuse safe path helper from inspect |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: JSON API + static server</name>
  <files>truth/serve.py, truth/static/inspector.html, truth/cli.py</files>
  <read_first>
    - truth/inspect/tree.py
    - truth/inspect/links.py
    - truth/inspect/changes.py
    - truth/inspect/graph.py
    - truth/cli.py
  </read_first>
  <action>
- `truth/serve.py`: stdlib HTTPServer on 127.0.0.1:8765 (port via --port)
- Routes: GET /api/tree, /api/links?path=, /api/changes?n=, /api/graph; GET / and /inspector.html serve static HTML
- `truth/cli.py`: add `serve` subcommand with --port
- `truth/static/inspector.html`: minimal dark UI, three panels, fetch API on load; click tree item loads links
- No markdown viewer (v2 deferred)
  </action>
  <acceptance_criteria>
    - truth/serve.py contains `127.0.0.1`
    - truth/static/inspector.html contains `/api/tree`
    - `truth serve` starts without import errors (smoke: curl /api/tree after background start)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `truth serve` binds localhost
2. curl /api/tree returns JSON array
3. /inspector.html loads and references API endpoints
</verification>

<success_criteria>
- INSPECT-05 satisfied
- No React/build step; single static HTML file
</success_criteria>
