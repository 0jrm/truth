---
phase: 04-memory-inspector
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - truth/cli.py
  - truth/inspect/__init__.py
  - truth/inspect/tree.py
  - truth/inspect/links.py
  - truth/inspect/changes.py
  - truth/inspect/graph.py
  - pyproject.toml
autonomous: true
requirements:
  - INSPECT-01
  - INSPECT-02
  - INSPECT-03
  - INSPECT-04
user_setup: []
must_haves:
  truths:
    - "truth tree prints notes hierarchy with type/title from frontmatter"
    - "truth links shows incoming and outgoing edges for a note"
    - "truth changes lists recent ops from events table"
    - "truth graph --json outputs valid nodes/edges JSON"
  artifacts:
    - path: truth/cli.py
      provides: "CLI entry point with tree/links/changes/graph subcommands"
      contains: "def main"
    - path: truth/inspect/tree.py
      provides: "File tree with frontmatter metadata"
      contains: "def build_tree"
  key_links:
    - from: truth/inspect/links.py
      to: truth/store/links.py
      via: "extract_links for edge scan"
      pattern: "extract_links"
    - from: truth/inspect/changes.py
      to: truth/index/events.py
      via: "list_events"
      pattern: "list_events"
---

<objective>
CLI inspector commands: tree, links, changes, graph --json. Stdlib argparse + existing store/index modules.

Purpose: Human observability over markdown truth and events index.
Output: `truth` console script with four inspector subcommands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@truth/store/links.py
@truth/store/frontmatter.py
@truth/store/paths.py
@truth/index/events.py
@truth/index/db.py
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path traversal via links arg | Medium | Resolve path under notes_root |
| SQL injection in changes | Low | Parameterized queries via list_events |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: inspect module (tree, links, changes, graph)</name>
  <files>truth/inspect/__init__.py, truth/inspect/tree.py, truth/inspect/links.py, truth/inspect/changes.py, truth/inspect/graph.py</files>
  <read_first>
    - truth/store/links.py
    - truth/store/frontmatter.py
    - truth/store/paths.py
    - truth/index/events.py
    - truth/index/db.py
  </read_first>
  <action>
Create `truth/inspect/` package:
- `tree.py`: `build_tree(root) -> list[dict]` with rel_path, type, title; `format_tree()` for CLI text
- `links.py`: `note_links(rel_path) -> {outgoing, incoming}` scanning all .md under root via extract_links
- `changes.py`: `recent_changes(n=20)` wrapping list_events(open_db())
- `graph.py`: `build_graph() -> {nodes, edges}` with rel paths as ids
- Safe path helper: resolve rel path under notes_root, reject traversal
- `if __name__` self-check in graph.py or tree.py
  </action>
  <acceptance_criteria>
    - truth/inspect/graph.py contains `def build_graph`
    - truth/inspect/tree.py contains `def build_tree`
    - python -m truth.inspect.graph exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CLI entry point</name>
  <files>truth/cli.py, pyproject.toml</files>
  <read_first>
    - truth/inspect/tree.py
    - truth/inspect/links.py
    - truth/inspect/changes.py
    - truth/inspect/graph.py
    - pyproject.toml
  </read_first>
  <action>
- `truth/cli.py`: argparse subcommands tree, links, changes (-n default 20), graph (--json)
- Register `[project.scripts] truth = "truth.cli:main"` in pyproject.toml
- pip install -e . so truth command works
  </action>
  <acceptance_criteria>
    - pyproject.toml contains `truth = "truth.cli:main"`
    - `truth tree` prints type/title for notes/*.md
    - `truth graph --json` prints valid JSON with nodes and edges keys
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `truth tree` shows sample notes with type and title
2. `truth links notes/hybrid-search.md` shows outgoing to sqlite-vectors.md
3. `truth changes -n 5` runs without error
4. `truth graph --json | python -m json.tool` succeeds
</verification>

<success_criteria>
- INSPECT-01 through INSPECT-04 satisfied
- No direct SQLite writes from inspector (read-only)
</success_criteria>
