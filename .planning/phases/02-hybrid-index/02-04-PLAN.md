---
phase: 02-hybrid-index
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - truth/index/events.py
  - truth/index/indexer.py
  - truth/index/watcher.py
autonomous: true
requirements:
  - IDX-06
user_setup: []
must_haves:
  truths:
    - "index_file writes create or update event with ISO timestamp"
    - "delete_file_from_index writes delete event"
    - "list_events(conn, n=20) returns recent ops newest-first"
  artifacts:
    - path: truth/index/events.py
      provides: "Events table append + query"
      contains: "def record_event"
  key_links:
    - from: truth/index/indexer.py
      to: truth/index/events.py
      via: "record on index/delete"
      pattern: "record_event"
---

<objective>
Events table for machine-readable changelog — create/update/delete on every index operation.

Purpose: Phase 4 `truth changes` reads this table; replaces unreliable file mtime.
Output: `record_event`, `list_events` API wired into indexer and watcher delete path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hybrid-index/02-CONTEXT.md
@.planning/phases/02-hybrid-index/02-RESEARCH.md
@truth/index/indexer.py
@truth/index/watcher.py
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Event log tampering | Low | Local single-user; DB file permissions only |
| Unbounded events table growth | Low | ponytail: no rotation in v1; Phase 4 may add -n limit only |

Block on: high — none.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Events module</name>
  <files>truth/index/events.py</files>
  <action>
`record_event(conn, path: str, op: str) -> None` — op in create|update|delete; ts = UTC ISO8601.

`list_events(conn, n: int = 20) -> list[dict]` — `SELECT path, op, ts FROM events ORDER BY id DESC LIMIT ?`.

Use relative path from notes_root where possible for stable display.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire events into indexer and watcher</name>
  <files>truth/index/indexer.py, truth/index/watcher.py</files>
  <action>
In `index_file`:
- If file row existed → `update` event; else `create` event
- Record inside same transaction as chunk upsert

In `delete_file_from_index`:
- `delete` event before commit

Add `if __name__ == '__main__'` in events.py: print last 5 events after a sample index run.

Export `list_events` from `truth.index`.
  </action>
  <verify>
    <automated>python -c "from truth.index.events import record_event, list_events; print('ok')"</automated>
  </verify>
</task>

</tasks>

<verification>
1. `index_all()` produces create events for seed notes (first run)
2. Re-index after edit produces update event
3. Delete produces delete event
4. `list_events(n=5)` returns expected ops
5. ROADMAP criterion 6 satisfied
</verification>

<success_criteria>
- IDX-06 satisfied
- Events durable across indexer restarts
</success_criteria>
