---
phase: 02-hybrid-index
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - truth/index/__init__.py
  - truth/index/db.py
  - truth/index/hashutil.py
  - truth/index/chunking.py
  - truth/index/embeddings.py
  - truth/index/indexer.py
autonomous: true
requirements:
  - IDX-01
  - IDX-02
  - IDX-03
  - IDX-05
user_setup: []
must_haves:
  truths:
    - "pip install -e . pulls sqlite-vec, sentence-transformers, watchdog"
    - "Opening memory.db loads sqlite-vec extension"
    - "index_all() walks notes/ and populates chunks + FTS + vec tables"
    - "Re-running index_all on unchanged files skips re-embedding (hash match)"
    - "Each chunk row links FTS and vec virtual tables by shared rowid"
  artifacts:
    - path: truth/index/db.py
      provides: "DB connection, schema migration, sqlite-vec load"
      contains: "def open_db"
    - path: truth/index/indexer.py
      provides: "Full and per-file index loop"
      contains: "def index_all"
    - path: truth/index/embeddings.py
      provides: "Lazy-loaded MiniLM embedder"
      contains: "all-MiniLM-L6-v2"
    - path: truth/index/chunking.py
      provides: "Paragraph chunker with overlap"
      contains: "def chunk_text"
  key_links:
    - from: truth/index/indexer.py
      to: truth/store/frontmatter.py
      via: "parse_note_file for body + meta"
      pattern: "parse_note_file"
    - from: truth/index/indexer.py
      to: truth/index/db.py
      via: "transactional upsert"
      pattern: "open_db"
---

<objective>
SQLite schema, paragraph chunking, local embedding integration, and full-folder indexer with content-hash skip.

Purpose: Foundation for hybrid search and watcher — every later Phase 2 plan imports this layer.
Output: `truth.index` package that can `index_all()` over `notes/` into `memory.db`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hybrid-index/02-CONTEXT.md
@.planning/phases/02-hybrid-index/02-RESEARCH.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@truth/store/frontmatter.py
@truth/store/paths.py
@VISION.MD
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| SQL injection via note paths | Low | Parameterized queries only; paths from filesystem walk |
| Path traversal outside notes root | Low | Index only files under `notes_root().resolve()` |
| Resource exhaustion (huge file) | Low | ponytail: skip files > 1MB with warning |
| Pickle/YAML in embed pipeline | N/A | Embeddings from text only |

Block on: high — none unmitigated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies and package scaffold</name>
  <files>pyproject.toml, truth/index/__init__.py</files>
  <action>
Add to `pyproject.toml` dependencies:
- `sqlite-vec>=0.1.6`
- `sentence-transformers>=3.0`
- `watchdog>=4.0`

Create `truth/index/__init__.py` exporting `index_all`, `index_file`, `open_db` (stubs until implemented, then wire).
  </action>
  <verify>
    <automated>pip install -e . && python -c "import truth.index; import sqlite_vec"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: DB schema and connection helper</name>
  <files>truth/index/db.py, truth/index/hashutil.py</files>
  <action>
`hashutil.py`: `content_hash(data: bytes) -> str` using SHA-256 hex.

`db.py`:
- `db_path()` from `TRUTH_DB_PATH` env or default `memory.db` in cwd
- `open_db()` → sqlite3.Connection with `sqlite_vec.load(conn)`
- `init_schema(conn)` creates `files`, `chunks`, `chunks_fts` (fts5 trigram), `chunks_vec` (vec0 float[384]), `events` per RESEARCH.md
- `PRAGMA foreign_keys = ON` if using FK helpers; else document rowid sync discipline
  </action>
  <verify>
    <automated>python -c "from truth.index.db import open_db, init_schema; c=open_db(); init_schema(c); print('ok')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Chunking and embeddings</name>
  <files>truth/index/chunking.py, truth/index/embeddings.py</files>
  <action>
`chunking.py`: `chunk_text(body: str, target: int = 512, overlap: int = 64) -> list[str]` — split on blank lines first, subdivide long paragraphs.

`embeddings.py`: lazy singleton `SentenceTransformer('all-MiniLM-L6-v2')`; `embed_texts(texts) -> list[list[float]]` with normalized embeddings; ponytail: module-level lock for thread safety.
  </action>
  <verify>
    <automated>python -c "from truth.index.chunking import chunk_text; assert len(chunk_text('a\n\nb'))>=1"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Indexer core loop</name>
  <files>truth/index/indexer.py</files>
  <action>
Implement:
- `index_file(conn, path: Path) -> None` — transactional per-file update
- `index_all(notes_root: Path | None = None) -> int` — walk `*.md`, skip non-notes, skip invalid frontmatter with `warnings.warn`
- Hash skip: compare `files.content_hash` before work
- On success: delete old chunks for path, insert new rows into chunks + chunks_fts + chunks_vec with matching rowids
- Store `note_type`, `note_title` from frontmatter meta
- Do NOT write events yet (Plan 02-04)

Add `if __name__ == '__main__'` self-check: index_all on sample notes, print chunk count.
  </action>
  <verify>
    <automated>python -m truth.index.indexer 2>&1 | grep -q chunk</automated>
  </verify>
</task>

</tasks>

<verification>
1. `pip install -e .` succeeds with new deps
2. `python -m truth.index.indexer` indexes 3 seed notes
3. Second run completes faster / logs skip for unchanged files
4. `sqlite3 memory.db "SELECT COUNT(*) FROM chunks"` returns > 0
</verification>

<success_criteria>
- IDX-01, IDX-02, IDX-03, IDX-05 satisfied for full-folder index path
- No markdown files modified by indexer
- Schema matches 384-dim embedding model
</success_criteria>
