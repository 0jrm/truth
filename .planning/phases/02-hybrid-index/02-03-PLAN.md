---
phase: 02-hybrid-index
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - truth/index/watcher.py
  - truth/index/__init__.py
autonomous: true
requirements:
  - IDX-04
user_setup: []
must_haves:
  truths:
    - "Editing a note triggers re-index of only that file within debounce window"
    - "Deleting a note removes its chunks from the index"
    - "Watcher ignores changes outside notes/"
  artifacts:
    - path: truth/index/watcher.py
      provides: "Debounced watchdog observer"
      contains: "def start_watcher"
  key_links:
    - from: truth/index/watcher.py
      to: truth/index/indexer.py
      via: "index_file on change"
      pattern: "index_file"
---

<objective>
Watchdog-driven incremental re-indexing with 500ms debounce — only affected files re-embedded.

Purpose: Agents and humans edit markdown; index stays current without full re-walk.
Output: `start_watcher()` blocking loop (Phase 5 integrates into `truth serve`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hybrid-index/02-CONTEXT.md
@.planning/phases/02-hybrid-index/02-RESEARCH.md
@truth/index/indexer.py
@truth/store/paths.py
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Symlink escape via watched tree | Low | Resolve paths; only index if under notes_root |
| Watcher thread crash | Low | Log exception; keep observer running |

Block on: high — none.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Debounced handler</name>
  <files>truth/index/watcher.py</files>
  <action>
Implement `_DebouncedIndexer`:
- On created/modified: schedule `index_file` after 500ms (cancel prior timer for same path)
- On deleted: call `delete_file_from_index(conn, path)` — remove chunks + files row (add to indexer.py if missing)
- Use `watchdog.observers.Observer` on `notes_root()`
- Ignore non-`.md` files and hidden files

`start_watcher(block: bool = True)` opens DB, starts observer, blocks if True.
  </action>
</task>

<task type="auto">
  <name>Task 2: Delete path + self-check</name>
  <files>truth/index/indexer.py, truth/index/watcher.py</files>
  <action>
Add `delete_file_from_index(conn, path)` — transactional chunk removal + files row delete.

Self-check script (not long-running): create temp note, index, modify, verify chunk text updates; delete, verify chunks gone. Use tempfile under notes/ or a `notes/_watcher_test.md` cleaned up after.

ponytail: self-check may use manual index_file calls instead of full watchdog loop in CI.
  </action>
  <verify>
    <automated>python -c "from truth.index.indexer import delete_file_from_index, open_db; print('ok')"</automated>
  </verify>
</task>

</tasks>

<verification>
1. Modify `notes/hybrid-search.md` body → re-index updates chunk text in DB
2. Delete a test note → chunk count decreases
3. ROADMAP criteria 2 and 3 pass
</verification>

<success_criteria>
- IDX-04 satisfied
- Debounce prevents double-index on rapid saves
</success_criteria>
