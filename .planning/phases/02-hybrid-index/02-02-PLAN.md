---
phase: 02-hybrid-index
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - truth/index/search.py
  - truth/index/__init__.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
user_setup: []
must_haves:
  truths:
    - "memory_search('sqlite', k=3) returns relevant chunks from seed notes"
    - "Results include path, text, and fused score"
    - "Vector-only and FTS-only both contribute (hybrid beats either alone on keyword+semantic query)"
  artifacts:
    - path: truth/index/search.py
      provides: "Hybrid search with RRF merge"
      contains: "def memory_search"
  key_links:
    - from: truth/index/search.py
      to: truth/index/embeddings.py
      via: "query embedding"
      pattern: "embed_texts"
    - from: truth/index/search.py
      to: truth/index/db.py
      via: "FTS MATCH + vec MATCH"
      pattern: "open_db"
---

<objective>
BM25 keyword search via FTS5, vector KNN via sqlite-vec, merged with RRF into `memory_search(query, k)`.

Purpose: Delivers searchable memory — core value of Phase 2; Phase 3 re-exports this API.
Output: Callable `memory_search` returning ranked chunk dicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hybrid-index/02-CONTEXT.md
@.planning/phases/02-hybrid-index/02-RESEARCH.md
@truth/index/indexer.py
@truth/index/db.py
@notes/hybrid-search.md
</context>

<threat_model>
## Threat Model (ASVS L1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| FTS query injection | Low | Sanitize: wrap query terms; reject raw `'` by escaping per FTS5 rules or tokenize to alnum tokens |
| Unbounded result sets | Low | Cap per-channel retrieval at 50 before RRF |

Block on: high — none.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: FTS and vector retrieval helpers</name>
  <files>truth/index/search.py</files>
  <action>
Add private helpers:
- `_fts_search(conn, query, limit=50) -> dict[rowid, rank]` using `chunks_fts WHERE text MATCH ? ORDER BY rank`
- `_vec_search(conn, query_vec, limit=50) -> dict[rowid, distance]` using sqlite-vec `MATCH ? AND k = ?`

Join rowids to `chunks` for path, text, chunk_index, note_type.
  </action>
</task>

<task type="auto">
  <name>Task 2: RRF merge and memory_search API</name>
  <files>truth/index/search.py, truth/index/__init__.py</files>
  <action>
Implement `rrf_merge(fts_ranks, vec_ranks, k=60)` per RESEARCH.md.

`memory_search(query: str, k: int = 5) -> list[dict]`:
1. Embed query (single string)
2. Run FTS + vector searches
3. RRF merge
4. Return top-k dicts: `{path, text, score, chunk_index, note_type}`

Export from `truth.index` and `truth.index.search`.

Add `if __name__ == '__main__'`: run `memory_search('hybrid BM25 vector')` and print top 3 paths.
  </action>
  <verify>
    <automated>python -m truth.index.search 2>&1 | grep -q hybrid-search</automated>
  </verify>
</task>

</tasks>

<verification>
1. After indexing seed notes, `memory_search('OKF')` returns okf-format.md chunk
2. `memory_search('RRF reciprocal rank fusion')` returns hybrid-search.md chunk
3. Each result dict has path, text, score keys
</verification>

<success_criteria>
- SRCH-01 through SRCH-04 met
- ROADMAP criterion 4 (`memory_search` returns hybrid results) satisfied
</success_criteria>
